# =============================================================================
# Variables d'environnement — World News
# =============================================================================
# Ce fichier liste les variables attendues par docker-compose.yml.
# Pour l'utiliser :
#   1. Copier ce fichier : cp .env.example .env
#   2. Remplir les valeurs dans .env
#   3. Ne JAMAIS committer le fichier .env (il est dans le .gitignore)

# --- PostgreSQL ---
POSTGRES_DB=worldnews
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_password_here

# --- Writer API ---
GEMINI_API_KEY=your_gemini_api_key_here
